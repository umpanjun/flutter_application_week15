import 'package:flutter/material.dart';
import 'ProductListScreen.dart'; // นำเข้าไฟล์ ProductListScreen

void main() {
  runApp(MaterialApp(
    home: ProductListScreen(), // ใช้ ProductListScreen เป็นหน้าแรก
  ));
}